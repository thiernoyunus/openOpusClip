scenedetect
ultralytics
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
tqdm
yt-dlp
faster-whisper==1.2.1
# faster-whisper enables its VAD filter by default in this app and declares
# onnxruntime as the runtime that executes the bundled Silero VAD model.
onnxruntime==1.19.2
# Intel builds currently resolve to a Torch wheel built against NumPy 1.x.
# Keep one known ABI-compatible NumPy version across Intel, Apple Silicon, and
# Windows instead of letting each machine choose a different major version.
numpy==1.26.4
google-genai==1.75.0
python-dotenv==1.2.2
mediapipe==0.10.14
boto3==1.43.4

# Auto-installs only on Apple Silicon for fast local transcription;
# pip skips it on Docker/Linux/Intel (no mlx wheels there), so the build never breaks.
mlx-whisper; sys_platform == "darwin" and platform_machine == "arm64"

fastapi==0.136.1
uvicorn==0.46.0
python-multipart==0.0.27
httpx==0.28.1
Pillow==12.2.0
keyring>=25.6,<26
mcp[cli]>=2.0.0,<3
